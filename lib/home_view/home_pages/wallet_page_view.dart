import 'package:flutter/material.dart';

class WalletPageView extends StatelessWidget {
  const WalletPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Wallet Row One'),
        Text('Wallet Row Two'),
        Text('Wallet Row Three'),
      ],
    );
  }
}
